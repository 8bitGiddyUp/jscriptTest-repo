"use strict";

const cal = require("./utils.js");

const sum = cal.add(4, -2);
console.log("sum: ", sum);

const sub = cal.substract(4, -2);
console.log("sub: ", sub);

const mul = cal.times(4, -2);
console.log("mul: ", mul);

/*
  puts this in utils.js
    const name = "Jack";
*/
// console.log(name);
